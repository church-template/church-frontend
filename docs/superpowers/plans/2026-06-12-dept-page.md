# 부서 소개 페이지(T09) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 부서 목록 `/departments`(그룹별 카드 섹션)와 상세 `/departments/[id]`(14B DeptHero 카드확장 + 인도자·본문·하위부서)를 공개 서버 컴포넌트 + ISR로 구현한다.

**Architecture:** 라우트는 `(site)` 그룹 밖 `src/app/departments/`에 두고 각 page가 셸을 직접 합성한다(목록=SiteShell 라이트, 상세=`SiteHeader transparent+solid` 고정 + DeptHero + CtaBand + Footer). 데이터는 `getDepartments`/`getDepartment` 병렬 fetch(revalidate 60) + `connection()`(CI 빌드), `buildDepartmentTree`로 `parentId` 트리 조립. 콘텐츠·미디어는 프론트 상수 주입(하드코딩 0).

**Tech Stack:** Next.js 16(App Router, `params: Promise`), TypeScript, Tailwind v4 토큰, Vitest 4 + @testing-library/react, lucide 불필요(카드 사진형).

**스펙:** `docs/superpowers/specs/2026-06-12-dept-page-design.md`

**기존 자산 재사용:** `Card`(ui), `Container`/`SiteShell`/`SiteHeader`/`CtaBand`/`SiteFooter`(shell), `EmptyState`/`MarkdownContent`(common), `Reveal`(main), `HeroMedia`(`@/hero/types`), `apiUrl`(`@/lib/auth/apiBase`), `typo`·`cn`.

**전제(범위 밖, §스펙 7):** 부서 CRUD·헤더 on-dark 투명 전환·형제 네비·media:{id} 동적 미디어·3단+ 깊은 트리. `/public/dept/*.jpg`는 배포 시 교체할 플레이스홀더(dev에서 깨진 이미지 정상 — 기존 `/hero.mp4`와 동일).

---

## Task 1: 부서 API 타입 · fetch · 트리 조립

**Files:**
- Modify: `src/lib/api/types.ts` (끝에 추가)
- Create: `src/lib/api/departments.ts`
- Test: `src/lib/api/departments.test.ts`

- [ ] **Step 1: 실패 테스트 작성** — `src/lib/api/departments.test.ts`

```ts
import { describe, it, expect, vi, afterEach } from "vitest";
import { getDepartments, getDepartment, buildDepartmentTree, bySortOrder } from "./departments";
import type { DepartmentCardResponse } from "./types";

afterEach(() => vi.unstubAllGlobals());

const okJson = (body: unknown) => ({ ok: true, status: 200, json: async () => body }) as Response;

const card = (over: Partial<DepartmentCardResponse>): DepartmentCardResponse => ({
  id: 1, name: "부서", leader: "", parentId: null, sortOrder: 0, ...over,
});

describe("getDepartments", () => {
  it("'/api/departments'를 revalidate 60으로 호출한다", async () => {
    const spy = vi.fn(async () => okJson([]));
    vi.stubGlobal("fetch", spy);
    await getDepartments();
    expect(spy).toHaveBeenCalledWith("/api/departments", { next: { revalidate: 60 } });
  });

  it("null 응답은 빈 배열로 방어한다", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => okJson(null)));
    expect(await getDepartments()).toEqual([]);
  });

  it("비 200이면 throw한다(error.tsx 위임)", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => ({ ok: false, status: 500 }) as Response));
    await expect(getDepartments()).rejects.toThrow("GET /api/departments 실패: 500");
  });
});

describe("getDepartment", () => {
  it("상세를 반환한다", async () => {
    const detail = { ...card({ id: 7, name: "청년부" }), description: "본문", createdAt: "", updatedAt: "", version: 1 };
    vi.stubGlobal("fetch", vi.fn(async () => okJson(detail)));
    expect(await getDepartment(7)).toMatchObject({ id: 7, name: "청년부", description: "본문" });
  });

  it("404는 null을 반환한다(미존재)", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => ({ ok: false, status: 404 }) as Response));
    expect(await getDepartment(999)).toBeNull();
  });

  it("그 외 비 200은 throw한다", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => ({ ok: false, status: 500 }) as Response));
    await expect(getDepartment(7)).rejects.toThrow("GET /api/departments/7 실패: 500");
  });
});

describe("buildDepartmentTree", () => {
  it("parentId로 자식을 중첩하고 sortOrder,id로 정렬한다", () => {
    const list = [
      card({ id: 1, name: "교회학교", sortOrder: 1 }),
      card({ id: 3, name: "초등부", parentId: 1, sortOrder: 2 }),
      card({ id: 2, name: "유치부", parentId: 1, sortOrder: 1 }),
    ];
    const roots = buildDepartmentTree(list);
    expect(roots.map((r) => r.id)).toEqual([1]);
    expect(roots[0].children.map((c) => c.name)).toEqual(["유치부", "초등부"]);
  });

  it("부모 없는 parentId(고아)는 루트로 승격한다", () => {
    const roots = buildDepartmentTree([card({ id: 5, name: "고아", parentId: 99 })]);
    expect(roots.map((r) => r.id)).toEqual([5]);
  });

  it("입력 배열을 변형하지 않는다(불변)", () => {
    const list = [card({ id: 1 }), card({ id: 2, parentId: 1 })];
    const snapshot = JSON.stringify(list);
    buildDepartmentTree(list);
    expect(JSON.stringify(list)).toBe(snapshot);
  });

  it("bySortOrder는 sortOrder 후 id로 비교한다", () => {
    expect(bySortOrder(card({ id: 2, sortOrder: 1 }), card({ id: 1, sortOrder: 1 }))).toBeGreaterThan(0);
    expect(bySortOrder(card({ id: 1, sortOrder: 1 }), card({ id: 9, sortOrder: 2 }))).toBeLessThan(0);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/lib/api/departments.test.ts`
Expected: FAIL — `Failed to resolve import "./departments"` / 타입 미존재.

- [ ] **Step 3: 타입 추가** — `src/lib/api/types.ts` 끝에 append

```ts

export interface DepartmentCardResponse {
  id: number;
  name: string;
  leader: string;
  parentId: number | null;
  sortOrder: number;
}

export interface DepartmentDetailResponse extends DepartmentCardResponse {
  description: string;
  createdAt: string; // LocalDateTime
  updatedAt: string;
  version: number; // 낙관적 락
}

export interface DepartmentNode extends DepartmentCardResponse {
  children: DepartmentNode[];
}
```

- [ ] **Step 4: fetch·트리 구현** — `src/lib/api/departments.ts`

```ts
import { apiUrl } from "@/lib/auth/apiBase";
import type {
  DepartmentCardResponse,
  DepartmentDetailResponse,
  DepartmentNode,
} from "./types";

// 부서 조회(공개, 인증 불필요) — 서버 컴포넌트 전용. revalidate 60 = main.ts와 동일 캐시 정책.
export async function getDepartments(): Promise<DepartmentCardResponse[]> {
  const res = await fetch(apiUrl("/api/departments"), { next: { revalidate: 60 } });
  if (!res.ok) {
    throw new Error(`GET /api/departments 실패: ${res.status}`);
  }
  // 비페이징 평배열(정렬 sortOrder,id는 서버 보장). null 방어.
  return ((await res.json()) as DepartmentCardResponse[] | null) ?? [];
}

export async function getDepartment(id: number): Promise<DepartmentDetailResponse | null> {
  const res = await fetch(apiUrl(`/api/departments/${id}`), { next: { revalidate: 60 } });
  if (res.status === 404) {
    return null; // 미존재 → 페이지가 notFound()로 처리
  }
  if (!res.ok) {
    throw new Error(`GET /api/departments/${id} 실패: ${res.status}`);
  }
  return (await res.json()) as DepartmentDetailResponse;
}

// 정렬 비교자 — 트리 조립·상세 자식 정렬 공용(서버 정렬 방어 + 트리 재정렬).
export const bySortOrder = (
  a: DepartmentCardResponse,
  b: DepartmentCardResponse,
): number => a.sortOrder - b.sortOrder || a.id - b.id;

// 평배열 → parentId 트리. 입력 불변(새 노드 생성). 부모 미존재(고아)는 루트로 승격.
export function buildDepartmentTree(
  list: DepartmentCardResponse[],
): DepartmentNode[] {
  const byId = new Map<number, DepartmentNode>();
  for (const d of list) {
    byId.set(d.id, { ...d, children: [] });
  }
  const roots: DepartmentNode[] = [];
  for (const node of byId.values()) {
    const parent = node.parentId != null ? byId.get(node.parentId) : undefined;
    if (parent) {
      parent.children.push(node);
    } else {
      roots.push(node);
    }
  }
  for (const node of byId.values()) {
    node.children.sort(bySortOrder);
  }
  roots.sort(bySortOrder);
  return roots;
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `pnpm vitest run src/lib/api/departments.test.ts`
Expected: PASS (11 tests).

- [ ] **Step 6: 커밋**

```bash
git add src/lib/api/types.ts src/lib/api/departments.ts src/lib/api/departments.test.ts
git commit -m "feat: 부서 API 타입·fetch·parentId 트리 조립"
```

---

## Task 2: 부서 콘텐츠 상수 (미디어·캡션·카피)

**Files:**
- Create: `src/constants/departments.ts`
- Test: `src/constants/departments.test.ts`

- [ ] **Step 1: 실패 테스트 작성** — `src/constants/departments.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { DEPT_FALLBACK, DEPT_PAGE, deptContent, deptThumbnail, thumbnailOf } from "./departments";

describe("부서 콘텐츠 상수", () => {
  it("미매핑 id는 폴백 콘텐츠를 반환한다", () => {
    expect(deptContent(123456)).toBe(DEPT_FALLBACK);
  });

  it("미매핑 id 썸네일은 기본 에셋이다", () => {
    expect(deptThumbnail(123456)).toBe("/dept/default.jpg");
  });

  it("thumbnailOf: image는 src, video는 poster, poster 없으면 기본", () => {
    expect(thumbnailOf({ type: "image", src: "/dept/1.jpg" })).toBe("/dept/1.jpg");
    expect(thumbnailOf({ type: "video", src: "/v.mp4", poster: "/p.jpg" })).toBe("/p.jpg");
    expect(thumbnailOf({ type: "video", src: "/v.mp4" })).toBe("/dept/default.jpg");
  });

  it("페이지 카피 상수를 노출한다(하드코딩 0)", () => {
    expect(DEPT_PAGE.title.length).toBeGreaterThan(0);
    expect(DEPT_PAGE.leaderLabel.length).toBeGreaterThan(0);
    expect(DEPT_PAGE.subHeading.length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/constants/departments.test.ts`
Expected: FAIL — `Failed to resolve import "./departments"`.

- [ ] **Step 3: 상수 구현** — `src/constants/departments.ts`

```ts
// 부서 콘텐츠 주입 — 부서 API에 대표이미지·캡션 필드가 없으므로(백엔드 H) 프론트 상수로 둔다.
// 사용자 노출 텍스트·미디어는 전부 여기서 주입(하드코딩 금지, 가이드 12장). 배포 시 이 파일만 교체.
import type { HeroMedia } from "@/hero/types";

export interface DeptContent {
  media: HeroMedia;
  caption: string[]; // 풀스크린 확장 후 등장 카피 — 줄 단위 배열("\n" 불필요)
}

const FALLBACK_THUMB = "/dept/default.jpg";

// 부서 id → 히어로 미디어·캡션. 에셋은 /public/dept/{id}.jpg 관례(배포 시 채움).
// 예) 7: { media: { type: "image", src: "/dept/7.jpg", alt: "청년부" }, caption: ["믿음으로 자라는 다음세대"] }
export const DEPT_CONTENT: Record<number, DeptContent> = {};

export const DEPT_FALLBACK: DeptContent = {
  media: { type: "image", src: FALLBACK_THUMB, alt: "" },
  caption: ["함께 자라가는 공동체"],
};

// 목록·상세 페이지 카피.
export const DEPT_PAGE = {
  eyebrow: "교육부서",
  title: "교육부서 안내",
  empty: "등록된 부서가 없습니다.",
  leaderLabel: "인도",
  subHeading: "하위 부서",
} as const;

export function deptContent(id: number): DeptContent {
  return DEPT_CONTENT[id] ?? DEPT_FALLBACK;
}

// 카드 16:9 썸네일 — 영상이면 poster, 없으면 기본 에셋. 순수 함수(테스트 용이).
export function thumbnailOf(media: HeroMedia): string {
  return media.type === "image" ? media.src : (media.poster ?? FALLBACK_THUMB);
}

export function deptThumbnail(id: number): string {
  return thumbnailOf(deptContent(id).media);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm vitest run src/constants/departments.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: 커밋**

```bash
git add src/constants/departments.ts src/constants/departments.test.ts
git commit -m "feat: 부서 히어로 미디어·캡션·페이지 카피 상수"
```

---

## Task 3: DeptHero (14B 카드 확장 히어로)

**Files:**
- Create: `src/hero/DeptHero.tsx` (이슈 14B.4 검증 코드 — 로직 변경 금지)
- Create: `src/hero/DeptHero.module.css`
- Test: `src/hero/DeptHero.test.tsx`

- [ ] **Step 1: 실패 테스트 작성** — `src/hero/DeptHero.test.tsx`

```tsx
import { describe, it, expect, vi, afterEach } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import DeptHero from "./DeptHero";

afterEach(() => vi.unstubAllGlobals());

// reduced-motion으로 고정 → useEffect의 스크롤 셋업을 건너뛴다(렌더만 검증).
function reducedMotion() {
  vi.stubGlobal("matchMedia", vi.fn(() => ({ matches: true })));
}

describe("DeptHero", () => {
  it("이미지 미디어: 제목·캡션·img(src/alt)를 렌더한다", () => {
    reducedMotion();
    const { container } = render(
      <DeptHero title="청년부" caption={<span>믿음으로</span>} media={{ type: "image", src: "/dept/7.jpg", alt: "청년부" }} />,
    );
    expect(screen.getByText("청년부")).toBeDefined();
    expect(screen.getByText("믿음으로")).toBeDefined();
    const img = container.querySelector("img");
    expect(img?.getAttribute("src")).toBe("/dept/7.jpg");
    expect(img?.getAttribute("alt")).toBe("청년부");
  });

  it("영상 미디어: video를 렌더하고 onError 시 poster img로 폴백한다", () => {
    reducedMotion();
    const { container } = render(
      <DeptHero title="t" caption={<span>c</span>} media={{ type: "video", src: "/v.mp4", poster: "/p.jpg" }} />,
    );
    const video = container.querySelector("video");
    expect(video?.getAttribute("src")).toBe("/v.mp4");
    fireEvent.error(video!);
    expect(container.querySelector("video")).toBeNull();
    expect(container.querySelector("img")?.getAttribute("src")).toBe("/p.jpg");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/hero/DeptHero.test.tsx`
Expected: FAIL — `Failed to resolve import "./DeptHero"`.

- [ ] **Step 3: 컴포넌트 구현** — `src/hero/DeptHero.tsx` (이슈 14B.4 그대로, `HeroMedia` import만 `./types`)

```tsx
'use client';

import { useEffect, useRef, useState } from 'react';
import styles from './DeptHero.module.css';
import type { HeroMedia } from './types';

interface DeptHeroProps {
  title: string;
  caption: React.ReactNode;
  media: HeroMedia;
}

const lerp = (a: number, b: number, t: number) => a + (b - a) * t;
const clamp01 = (v: number) => Math.min(1, Math.max(0, v));
const segment = (p: number, s: number, e: number) => clamp01((p - s) / (e - s));

export default function DeptHero({ title, caption, media }: DeptHeroProps) {
  const heroRef = useRef<HTMLElement>(null);
  const stickyRef = useRef<HTMLDivElement>(null);
  const frameRef = useRef<HTMLDivElement>(null);
  const mediaRef = useRef<HTMLDivElement>(null);
  const titleRef = useRef<HTMLHeadingElement>(null);
  const captionRef = useRef<HTMLParagraphElement>(null);
  const [videoFailed, setVideoFailed] = useState(false);

  useEffect(() => {
    const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (reduced) return;

    const hero = heroRef.current!;
    const sticky = stickyRef.current!;
    const frame = frameRef.current!;
    const mediaEl = mediaRef.current!;
    const titleEl = titleRef.current!;
    const captionEl = captionRef.current!;

    let startInset = { top: 0, right: 0, bottom: 0, left: 0 };
    let ticking = false;

    const measure = () => {
      const s = sticky.getBoundingClientRect();
      const r = frame.getBoundingClientRect();
      startInset = {
        top: r.top - s.top,
        left: r.left - s.left,
        right: s.right - r.right,
        bottom: s.bottom - r.bottom, // 레이아웃상 0 (하단 패딩 없음)
      };
      update();
    };

    const update = () => {
      const total = hero.offsetHeight - window.innerHeight;
      const p = clamp01((window.scrollY - hero.offsetTop) / total);

      const pe = segment(p, 0, 0.55);
      const radius = lerp(24, 0, pe);
      mediaEl.style.clipPath =
        `inset(${lerp(startInset.top, 0, pe)}px ` +
        `${lerp(startInset.right, 0, pe)}px ` +
        `${lerp(startInset.bottom, 0, pe)}px ` +
        `${lerp(startInset.left, 0, pe)}px ` +
        `round ${radius}px ${radius}px 0 0)`;

      const pt = segment(p, 0, 0.35);
      titleEl.style.transform = `translateY(${lerp(0, -80, pt)}px)`;
      titleEl.style.opacity = String(1 - pt);

      const pc = segment(p, 0.6, 0.9);
      captionEl.style.opacity = String(pc);
      captionEl.style.transform = `translateY(calc(-50% + ${lerp(30, 0, pc)}px))`;

      ticking = false;
    };

    const onScroll = () => { if (!ticking) { requestAnimationFrame(update); ticking = true; } };
    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', measure);
    measure();
    return () => {
      window.removeEventListener('scroll', onScroll);
      window.removeEventListener('resize', measure);
    };
  }, []);

  return (
    <section ref={heroRef} className={styles.hero}>
      <div ref={stickyRef} className={styles.sticky}>
        <div className={styles.layout}>
          <h1 ref={titleRef} className={styles.title}>{title}</h1>
          <div ref={frameRef} className={styles.frame} aria-hidden="true" />
        </div>

        <div ref={mediaRef} className={styles.media}>
          {media.type === 'video' && !videoFailed ? (
            <video autoPlay muted loop playsInline preload="metadata"
              src={media.src} poster={media.poster}
              onError={() => setVideoFailed(true)} />
          ) : (
            // eslint-disable-next-line @next/next/no-img-element
            <img src={media.type === 'video' ? (media.poster ?? '') : media.src}
              alt={media.type === 'image' ? (media.alt ?? '') : ''} />
          )}
        </div>

        <p ref={captionRef} className={styles.caption}>{caption}</p>
      </div>
    </section>
  );
}
```

- [ ] **Step 4: 스타일 구현** — `src/hero/DeptHero.module.css` (이슈 14B.4 그대로 — 하드코딩 수치는 검증 코드 예외)

```css
/* DeptHero.module.css */
.hero { position: relative; height: 300vh; }
.sticky { position: sticky; top: 0; height: 100vh; height: 100dvh; overflow: hidden; }

.layout {
  position: relative; z-index: 2; height: 100%;
  width: 100%; max-width: 1200px; margin: 0 auto;  /* DESIGN.md layout 토큰과 일치 */
  padding: 14vh 24px 0;                            /* 하단 0: 카드가 바닥까지 */
  display: flex; flex-direction: column; pointer-events: none;
}
.title {
  font-size: clamp(36px, 6vw, 72px);
  font-weight: 500; letter-spacing: -0.02em;       /* DESIGN.md display-mega */
  margin: 0 0 5vh; will-change: transform, opacity;
}
.frame { flex: 1; visibility: hidden; }
.media {
  position: absolute; inset: 0; z-index: 1; will-change: clip-path;
  clip-path: inset(35% 24px 0 24px round 24px 24px 0 0); /* 첫 페인트 깜빡임 방지 근사값 */
}
.media video, .media img { width: 100%; height: 100%; object-fit: cover; display: block; }
.caption {
  position: absolute; top: 50%; z-index: 3;
  left: max(24px, calc((100vw - 1200px) / 2 + 24px));
  transform: translateY(calc(-50% + 30px));
  color: #fff; font-size: clamp(28px, 4.5vw, 56px);
  font-weight: 500; line-height: 1.4; letter-spacing: -0.02em;
  margin: 0; opacity: 0; will-change: transform, opacity;
  text-shadow: 0 2px 24px rgba(0, 0, 0, 0.25);
}
@media (prefers-reduced-motion: reduce) {
  .hero { height: auto; }
  .sticky { position: static; height: auto; }
  .layout { padding-bottom: 0; }
  .media { position: static; clip-path: none !important; height: 70vh; }
  .frame, .caption { display: none; }
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `pnpm vitest run src/hero/DeptHero.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 6: 커밋**

```bash
git add src/hero/DeptHero.tsx src/hero/DeptHero.module.css src/hero/DeptHero.test.tsx
git commit -m "feat: DeptHero 카드 확장 히어로(14B.4)"
```

---

## Task 4: DeptLeader (인도자 메타 라인)

**Files:**
- Create: `src/components/departments/DeptLeader.tsx`
- Test: `src/components/departments/DeptLeader.test.tsx`

- [ ] **Step 1: 실패 테스트 작성** — `src/components/departments/DeptLeader.test.tsx`

```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { DeptLeader } from "./DeptLeader";

describe("DeptLeader", () => {
  it("'인도 · {이름}' 형태로 렌더한다", () => {
    render(<DeptLeader name="김목사" />);
    expect(screen.getByText("인도 · 김목사")).toBeDefined();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/components/departments/DeptLeader.test.tsx`
Expected: FAIL — `Failed to resolve import "./DeptLeader"`.

- [ ] **Step 3: 구현** — `src/components/departments/DeptLeader.tsx`

```tsx
import { cn } from "@/lib/utils";
import { typo } from "@/constants/typography";
import { DEPT_PAGE } from "@/constants/departments";

// 부서 인도자 메타 — 1인이라 카드 대신 가벼운 한 줄(스펙 D3). 라벨은 상수 주입.
export function DeptLeader({ name }: { name: string }) {
  return (
    <p className={cn(typo.datetime, "text-muted")}>
      {DEPT_PAGE.leaderLabel} · {name}
    </p>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm vitest run src/components/departments/DeptLeader.test.tsx`
Expected: PASS (1 test).

- [ ] **Step 5: 커밋**

```bash
git add src/components/departments/DeptLeader.tsx src/components/departments/DeptLeader.test.tsx
git commit -m "feat: 부서 인도자 메타 라인"
```

---

## Task 5: DepartmentCard (목록·하위부서 공용 카드)

**Files:**
- Create: `src/components/departments/DepartmentCard.tsx`
- Test: `src/components/departments/DepartmentCard.test.tsx`

- [ ] **Step 1: 실패 테스트 작성** — `src/components/departments/DepartmentCard.test.tsx`

```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import type { ReactNode } from "react";

vi.mock("next/link", () => ({
  default: ({ href, children, ...rest }: { href: string; children: ReactNode }) => (
    <a href={href} {...rest}>{children}</a>
  ),
}));

import { DepartmentCard } from "./DepartmentCard";

const dept = { id: 7, name: "청년부", leader: "김목사", parentId: null, sortOrder: 0 };

describe("DepartmentCard", () => {
  it("이름·인도자·썸네일·상세 링크를 렌더한다", () => {
    const { container } = render(<DepartmentCard dept={dept} />);
    expect(screen.getByText("청년부")).toBeDefined();
    expect(screen.getByText("인도 · 김목사")).toBeDefined();
    expect(container.querySelector("img")?.getAttribute("src")).toBe("/dept/default.jpg");
    expect(screen.getByText("청년부").closest("a")?.getAttribute("href")).toBe("/departments/7");
  });

  it("인도자가 비면 인도 줄을 생략한다", () => {
    render(<DepartmentCard dept={{ ...dept, leader: "" }} />);
    expect(screen.queryByText(/인도 ·/)).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/components/departments/DepartmentCard.test.tsx`
Expected: FAIL — `Failed to resolve import "./DepartmentCard"`.

- [ ] **Step 3: 구현** — `src/components/departments/DepartmentCard.tsx` (SermonCard 인터랙티브 패턴 미러)

```tsx
import Link from "next/link";
import { Card } from "@/components/ui/Card";
import { cn } from "@/lib/utils";
import { typo } from "@/constants/typography";
import { DEPT_PAGE, deptThumbnail } from "@/constants/departments";
import type { DepartmentCardResponse } from "@/lib/api/types";

const focusRing =
  "focus-visible:outline-hidden focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-canvas rounded-xl";

// 부서 카드 — 16:9 부서 히어로 이미지(목록↔상세 연속성) + 이름 + 인도자. sermon-card 호버 재사용.
export function DepartmentCard({ dept }: { dept: DepartmentCardResponse }) {
  return (
    <Link href={`/departments/${dept.id}`} className={cn("block", focusRing)}>
      <Card bordered interactive className="group block h-full">
        <div className="aspect-video overflow-hidden">
          {/* eslint-disable-next-line @next/next/no-img-element -- 프레젠테이션 셸; next/image 교체는 후속 */}
          <img
            src={deptThumbnail(dept.id)}
            alt=""
            className="h-full w-full object-cover transition-transform duration-300 ease-out group-hover:scale-[1.03]"
          />
        </div>
        <div className="p-xl">
          <h3 className={cn(typo.titleMd, "text-ink")}>{dept.name}</h3>
          {dept.leader ? (
            <p className={cn(typo.datetime, "mt-xxs text-muted")}>
              {DEPT_PAGE.leaderLabel} · {dept.leader}
            </p>
          ) : null}
        </div>
      </Card>
    </Link>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm vitest run src/components/departments/DepartmentCard.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 5: 커밋**

```bash
git add src/components/departments/DepartmentCard.tsx src/components/departments/DepartmentCard.test.tsx
git commit -m "feat: 부서 카드(썸네일·이름·인도자·상세 링크)"
```

---

## Task 6: SubDepartments (상세 하위부서 그리드 + Reveal)

**Files:**
- Create: `src/components/departments/SubDepartments.tsx`
- Test: `src/components/departments/SubDepartments.test.tsx`

- [ ] **Step 1: 실패 테스트 작성** — `src/components/departments/SubDepartments.test.tsx`

```tsx
import { describe, it, expect, vi, afterEach } from "vitest";
import { render, screen } from "@testing-library/react";
import type { ReactNode } from "react";

vi.mock("next/link", () => ({
  default: ({ href, children, ...rest }: { href: string; children: ReactNode }) => (
    <a href={href} {...rest}>{children}</a>
  ),
}));

import { SubDepartments } from "./SubDepartments";
import { DEPT_PAGE } from "@/constants/departments";

afterEach(() => vi.unstubAllGlobals());

const items = [
  { id: 2, name: "유치부", leader: "", parentId: 1, sortOrder: 1 },
  { id: 3, name: "초등부", leader: "", parentId: 1, sortOrder: 2 },
];

describe("SubDepartments", () => {
  it("하위 부서 헤딩과 카드들을 렌더한다", () => {
    vi.stubGlobal("matchMedia", vi.fn(() => ({ matches: true }))); // Reveal IO 미등록
    render(<SubDepartments items={items} />);
    expect(screen.getByRole("heading", { name: DEPT_PAGE.subHeading })).toBeDefined();
    expect(screen.getByText("유치부").closest("a")?.getAttribute("href")).toBe("/departments/2");
    expect(screen.getByText("초등부")).toBeDefined();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/components/departments/SubDepartments.test.tsx`
Expected: FAIL — `Failed to resolve import "./SubDepartments"`.

- [ ] **Step 3: 구현** — `src/components/departments/SubDepartments.tsx`

```tsx
import { Container } from "@/components/shell/Container";
import { Reveal } from "@/components/main/Reveal";
import { DepartmentCard } from "./DepartmentCard";
import { typo } from "@/constants/typography";
import { cn } from "@/lib/utils";
import { DEPT_PAGE } from "@/constants/departments";
import type { DepartmentCardResponse } from "@/lib/api/types";

// 상세 하위부서 — soft 밴드 교차 리듬 + 카드별 Reveal 등장(스펙 D3).
export function SubDepartments({ items }: { items: DepartmentCardResponse[] }) {
  return (
    <section className="bg-surface-soft py-section">
      <Container>
        <h2 className={cn(typo.titleLg, "text-ink")}>{DEPT_PAGE.subHeading}</h2>
        <div className="mt-lg grid gap-base sm:grid-cols-2 lg:grid-cols-3">
          {items.map((d, i) => (
            <Reveal key={d.id} delay={i * 120} className="h-full">
              <DepartmentCard dept={d} />
            </Reveal>
          ))}
        </div>
      </Container>
    </section>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm vitest run src/components/departments/SubDepartments.test.tsx`
Expected: PASS (1 test).

- [ ] **Step 5: 커밋**

```bash
git add src/components/departments/SubDepartments.tsx src/components/departments/SubDepartments.test.tsx
git commit -m "feat: 상세 하위부서 그리드(Reveal 등장)"
```

---

## Task 7: DepartmentTree (목록 그룹별 카드 섹션)

**Files:**
- Create: `src/components/departments/DepartmentTree.tsx`
- Test: `src/components/departments/DepartmentTree.test.tsx`

- [ ] **Step 1: 실패 테스트 작성** — `src/components/departments/DepartmentTree.test.tsx`

```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import type { ReactNode } from "react";

vi.mock("next/link", () => ({
  default: ({ href, children, ...rest }: { href: string; children: ReactNode }) => (
    <a href={href} {...rest}>{children}</a>
  ),
}));

import { DepartmentTree } from "./DepartmentTree";
import type { DepartmentNode } from "@/lib/api/types";

const node = (over: Partial<DepartmentNode>): DepartmentNode => ({
  id: 1, name: "부서", leader: "", parentId: null, sortOrder: 0, children: [], ...over,
});

describe("DepartmentTree", () => {
  it("자식 있는 루트는 헤딩 링크 + 자식 카드, 리프 루트는 단일 카드", () => {
    const roots = [
      node({ id: 1, name: "교회학교", children: [node({ id: 2, name: "유치부", parentId: 1 })] }),
      node({ id: 9, name: "청년부" }), // 리프 루트
    ];
    render(<DepartmentTree roots={roots} />);
    // 루트 헤딩은 상세로 링크
    expect(screen.getByText("교회학교").closest("a")?.getAttribute("href")).toBe("/departments/1");
    // 자식 카드
    expect(screen.getByText("유치부").closest("a")?.getAttribute("href")).toBe("/departments/2");
    // 리프 루트 카드
    expect(screen.getByText("청년부").closest("a")?.getAttribute("href")).toBe("/departments/9");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/components/departments/DepartmentTree.test.tsx`
Expected: FAIL — `Failed to resolve import "./DepartmentTree"`.

- [ ] **Step 3: 구현** — `src/components/departments/DepartmentTree.tsx`

```tsx
import Link from "next/link";
import { DepartmentCard } from "./DepartmentCard";
import { typo } from "@/constants/typography";
import { cn } from "@/lib/utils";
import type { DepartmentNode } from "@/lib/api/types";

const grid = "mt-base grid gap-base sm:grid-cols-2 lg:grid-cols-3";

// 목록 = 그룹별 카드 섹션(스펙 D4). 자식 있는 루트는 섹션(헤딩 링크 + 자식 그리드),
// 자식 없는 루트(리프)는 말미 단일 그리드로 모아 단독 섹션 난립 방지.
export function DepartmentTree({ roots }: { roots: DepartmentNode[] }) {
  const parents = roots.filter((r) => r.children.length > 0);
  const leaves = roots.filter((r) => r.children.length === 0);

  return (
    <div className="mt-lg space-y-xxl">
      {parents.map((root) => (
        <section key={root.id} aria-label={root.name}>
          <Link
            href={`/departments/${root.id}`}
            className={cn(typo.titleLg, "text-ink hover:text-primary")}
          >
            {root.name}
          </Link>
          <div className={grid}>
            {root.children.map((c) => (
              <DepartmentCard key={c.id} dept={c} />
            ))}
          </div>
        </section>
      ))}
      {leaves.length > 0 ? (
        <div className={cn(grid, "mt-0")}>
          {leaves.map((r) => (
            <DepartmentCard key={r.id} dept={r} />
          ))}
        </div>
      ) : null}
    </div>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm vitest run src/components/departments/DepartmentTree.test.tsx`
Expected: PASS (1 test).

- [ ] **Step 5: 커밋**

```bash
git add src/components/departments/DepartmentTree.tsx src/components/departments/DepartmentTree.test.tsx
git commit -m "feat: 부서 목록 그룹별 카드 섹션(트리)"
```

---

## Task 8: 목록 페이지 `/departments`

**Files:**
- Create: `src/app/departments/page.tsx`
- Test: `src/app/departments/page.test.tsx`

- [ ] **Step 1: 실패 테스트 작성** — `src/app/departments/page.test.tsx`

```tsx
import { describe, it, expect, vi, afterEach } from "vitest";
import { render, screen } from "@testing-library/react";
import type { ReactNode } from "react";
import { DEPT_PAGE } from "@/constants/departments";

vi.mock("next/navigation", () => ({ usePathname: () => "/departments" }));
vi.mock("next/server", () => ({ connection: async () => {} }));
vi.mock("next/link", () => ({
  default: ({ href, children, ...rest }: { href: string; children: ReactNode }) => (
    <a href={href} {...rest}>{children}</a>
  ),
}));
// getDepartments만 오버라이드, buildDepartmentTree/bySortOrder는 실제 사용
vi.mock("@/lib/api/departments", async (importOriginal) => ({
  ...(await importOriginal<typeof import("@/lib/api/departments")>()),
  getDepartments: vi.fn(),
}));

import DepartmentsPage from "./page";
import { getDepartments } from "@/lib/api/departments";

function stubBrowserApis() {
  vi.stubGlobal("matchMedia", vi.fn(() => ({ matches: true, addEventListener: () => {}, removeEventListener: () => {} })));
  vi.stubGlobal("IntersectionObserver", class { observe() {} unobserve() {} disconnect() {} });
}
afterEach(() => vi.unstubAllGlobals());

describe("DepartmentsPage (목록)", () => {
  it("부서 트리(헤딩 타이틀 + 카드)를 렌더한다", async () => {
    stubBrowserApis();
    vi.mocked(getDepartments).mockResolvedValue([
      { id: 1, name: "교회학교", leader: "", parentId: null, sortOrder: 1 },
      { id: 2, name: "유치부", leader: "박교사", parentId: 1, sortOrder: 1 },
    ]);
    render(await DepartmentsPage());
    expect(screen.getByRole("heading", { name: DEPT_PAGE.title })).toBeDefined();
    expect(screen.getByText("유치부").closest("a")?.getAttribute("href")).toBe("/departments/2");
  });

  it("빈 목록이면 플레이스홀더를 표시한다", async () => {
    stubBrowserApis();
    vi.mocked(getDepartments).mockResolvedValue([]);
    render(await DepartmentsPage());
    expect(screen.getByText(DEPT_PAGE.empty)).toBeDefined();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run src/app/departments/page.test.tsx`
Expected: FAIL — `Failed to resolve import "./page"`.

- [ ] **Step 3: 구현** — `src/app/departments/page.tsx`

```tsx
import { connection } from "next/server";
import { SiteShell } from "@/components/shell/SiteShell";
import { Container } from "@/components/shell/Container";
import { EmptyState } from "@/components/common/EmptyState";
import { DepartmentTree } from "@/components/departments/DepartmentTree";
import { getDepartments, buildDepartmentTree } from "@/lib/api/departments";
import { DEPT_PAGE } from "@/constants/departments";
import { typo } from "@/constants/typography";
import { cn } from "@/lib/utils";

// 부서 목록(공개) — 라이트 셸. prerender는 connection()으로 요청 시점 위임(CI 빌드, 스펙 D7).
export default async function DepartmentsPage() {
  await connection();
  const list = await getDepartments();
  const roots = buildDepartmentTree(list);

  return (
    <SiteShell>
      <Container as="section" className="py-section">
        <p className={cn(typo.caption, "text-center text-primary")}>{DEPT_PAGE.eyebrow}</p>
        <h1 className={cn(typo.displayMd, "mt-xs text-center text-ink")}>{DEPT_PAGE.title}</h1>
        {list.length === 0 ? (
          <EmptyState message={DEPT_PAGE.empty} className="mt-lg" />
        ) : (
          <DepartmentTree roots={roots} />
        )}
      </Container>
    </SiteShell>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm vitest run src/app/departments/page.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 5: 커밋**

```bash
git add src/app/departments/page.tsx src/app/departments/page.test.tsx
git commit -m "feat: 부서 목록 페이지 /departments"
```

---

## Task 9: 상세 페이지 `/departments/[id]`

**Files:**
- Create: `src/app/departments/[id]/page.tsx`
- Test: `src/app/departments/[id]/page.test.tsx`

- [ ] **Step 1: 실패 테스트 작성** — `src/app/departments/[id]/page.test.tsx`

```tsx
import { describe, it, expect, vi, afterEach } from "vitest";
import { render, screen } from "@testing-library/react";
import type { ReactNode } from "react";

vi.mock("next/navigation", () => ({
  usePathname: () => "/departments/7",
  notFound: () => { throw new Error("NEXT_NOT_FOUND"); },
}));
vi.mock("next/server", () => ({ connection: async () => {} }));
vi.mock("next/link", () => ({
  default: ({ href, children, ...rest }: { href: string; children: ReactNode }) => (
    <a href={href} {...rest}>{children}</a>
  ),
}));
// 히어로는 스크롤 연출 분리 — 페이지 합성만 검증(스펙 §6). 별도 Task 3에서 단위 검증.
vi.mock("@/hero/DeptHero", () => ({
  default: ({ title, caption }: { title: string; caption: ReactNode }) => (
    <div data-testid="hero"><h1>{title}</h1><div>{caption}</div></div>
  ),
}));
vi.mock("@/lib/api/departments", async (importOriginal) => ({
  ...(await importOriginal<typeof import("@/lib/api/departments")>()),
  getDepartments: vi.fn(),
  getDepartment: vi.fn(),
}));

import DepartmentDetailPage from "./page";
import { getDepartments, getDepartment } from "@/lib/api/departments";

function stubBrowserApis() {
  vi.stubGlobal("matchMedia", vi.fn(() => ({ matches: true, addEventListener: () => {}, removeEventListener: () => {} })));
  vi.stubGlobal("IntersectionObserver", class { observe() {} unobserve() {} disconnect() {} });
}
afterEach(() => vi.unstubAllGlobals());

const detail = { id: 7, name: "청년부", leader: "김목사", parentId: null, sortOrder: 0, description: "청년부 **소개**", createdAt: "", updatedAt: "", version: 1 };

describe("DepartmentDetailPage (상세)", () => {
  it("제목·인도자·본문·하위부서를 합성한다", async () => {
    stubBrowserApis();
    vi.mocked(getDepartment).mockResolvedValue(detail);
    vi.mocked(getDepartments).mockResolvedValue([
      detail,
      { id: 8, name: "대학부", leader: "", parentId: 7, sortOrder: 1 },
    ]);
    render(await DepartmentDetailPage({ params: Promise.resolve({ id: "7" }) }));
    expect(screen.getByRole("heading", { name: "청년부" })).toBeDefined();
    expect(screen.getByText("인도 · 김목사")).toBeDefined();
    expect(screen.getByText("소개")).toBeDefined(); // 마크다운 <strong>
    expect(screen.getByText("대학부").closest("a")?.getAttribute("href")).toBe("/departments/8");
  });

  it("부서가 없으면 notFound()", async () => {
    stubBrowserApis();
    vi.mocked(getDepartments).mockResolvedValue([]);
    vi.mocked(getDepartment).mockResolvedValue(null);
    await expect(
      DepartmentDetailPage({ params: Promise.resolve({ id: "999" }) }),
    ).rejects.toThrow("NEXT_NOT_FOUND");
  });

  it("id가 정수가 아니면 notFound()", async () => {
    stubBrowserApis();
    await expect(
      DepartmentDetailPage({ params: Promise.resolve({ id: "abc" }) }),
    ).rejects.toThrow("NEXT_NOT_FOUND");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm vitest run "src/app/departments/[id]/page.test.tsx"`
Expected: FAIL — `Failed to resolve import "./page"`.

- [ ] **Step 3: 구현** — `src/app/departments/[id]/page.tsx`

```tsx
import { connection } from "next/server";
import { notFound } from "next/navigation";
import { SiteHeader } from "@/components/shell/SiteHeader";
import { Container } from "@/components/shell/Container";
import { CtaBand } from "@/components/shell/CtaBand";
import { SiteFooter } from "@/components/shell/SiteFooter";
import { MarkdownContent } from "@/components/common/MarkdownContent";
import DeptHero from "@/hero/DeptHero";
import { DeptLeader } from "@/components/departments/DeptLeader";
import { SubDepartments } from "@/components/departments/SubDepartments";
import { getDepartments, getDepartment, bySortOrder } from "@/lib/api/departments";
import { deptContent } from "@/constants/departments";

// 부서 상세(공개) — SiteShell 대신 투명+solid 고정 헤더를 직접 합성(스펙 D2). DeptHero 시작이
// 라이트라 on-dark 투명은 가독성 위험 → 라이트 스킨 고정(IO 전환 없음, 메인 무수정).
export default async function DepartmentDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  await connection();
  const { id } = await params;
  const n = Number(id);
  if (!Number.isInteger(n)) notFound();

  // 목록 = 자식 계산용, 상세 = description. 병렬 fetch(스펙 D7).
  const [list, detail] = await Promise.all([getDepartments(), getDepartment(n)]);
  if (!detail) notFound();

  const content = deptContent(detail.id);
  // 줄 단위 카피 → block span(메인 caption과 동일 패턴, "\n" 이슈 차단).
  const caption = content.caption.map((line, i) => (
    <span key={i} className="block">
      {line}
    </span>
  ));
  const children = list.filter((d) => d.parentId === detail.id).sort(bySortOrder);

  return (
    <>
      <SiteHeader variant="transparent" solid />
      <main className="flex-1">
        <DeptHero title={detail.name} caption={caption} media={content.media} />
        <Container as="section" className="py-section">
          {detail.leader ? <DeptLeader name={detail.leader} /> : null}
          {detail.description ? (
            <MarkdownContent source={detail.description} className="mt-base" />
          ) : null}
        </Container>
        {children.length > 0 ? <SubDepartments items={children} /> : null}
      </main>
      <CtaBand />
      <SiteFooter />
    </>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm vitest run "src/app/departments/[id]/page.test.tsx"`
Expected: PASS (3 tests).

- [ ] **Step 5: 커밋**

```bash
git add "src/app/departments/[id]/page.tsx" "src/app/departments/[id]/page.test.tsx"
git commit -m "feat: 부서 상세 페이지 /departments/[id]"
```

---

## Task 10: DESIGN.md 컴포넌트 항목 추가

**Files:**
- Modify: `.claude/rules/DESIGN.md` (`### 연출` 블록 끝, `ministry-cards` 항목 뒤에 추가)

- [ ] **Step 1: 항목 추가** — 아래 텍스트를 `ministry-cards` 항목 다음에 삽입

```markdown
- **`dept-hero`**: 부서 상세 히어로(가이드 14B). 미디어 카드가 스크롤에 따라 `clip-path`로 풀스크린
  확장(width/height 애니메이션 금지), placeholder 측정으로 시작 inset 산출. 확장 종료(0.55) 후
  카피 페이드인(0.60~, 겹침 0.05 유지). reduced-motion은 정적 70vh. 검증 코드라 내부 수치(px·rgba·#fff)는
  토큰 인라인 금지의 예외. `HeroMedia`는 CrossHero와 공유(`@/hero/types`).
- **`department-card`**: 부서 카드(목록·하위부서 공용). 16:9 부서 히어로 이미지 썸네일 + 이름
  `{typography.title-md}` + 인도자 `{typography.datetime}` `{colors.muted}`. `sermon-card` 호버(soft drop +
  썸네일 1.03 줌) 재사용. 목록 카드 이미지 = 상세 히어로 이미지(목록↔상세 시각 연속성).
- **`dept-tree`**: 부서 목록 = 그룹별 카드 섹션(조직도 다이어그램 아님 — 탐색 목적·고령 터치·단일 액센트).
  자식 있는 루트 = 헤딩 링크 + 자식 카드 그리드, 리프 루트 = 말미 단일 그리드. `parentId` 트리 조립.
```

- [ ] **Step 2: 커밋**

```bash
git add .claude/rules/DESIGN.md
git commit -m "docs: DESIGN.md에 dept-hero·department-card·dept-tree 추가"
```

---

## Task 11: 전체 검증 (게이트: 14B.9 검수 기준)

**Files:** (없음 — 검증만)

- [ ] **Step 1: 전체 테스트**

Run: `pnpm vitest run`
Expected: PASS (FAIL 0). 기존 191 + 신규 약 27 테스트.

- [ ] **Step 2: 타입 체크**

Run: `pnpm exec tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 3: 린트**

Run: `pnpm lint`
Expected: 경고·에러 없음(이모지·hex·px 인라인·`&&` 렌더링 규칙 포함).

- [ ] **Step 4: 프로덕션 빌드**

Run: `pnpm build`
Expected: 성공. `/departments`·`/departments/[id]` 라우트 생성, prerender 오류 없음(connection() 효과).

- [ ] **Step 5: 수동 게이트(dev) — 14B.9 검수**

Run: `pnpm dev` 후 `/departments/1` 확인:
- 카드 좌우 여백이 헤드라인 시작점과 정렬, 하단이 뷰포트 바닥에 붙고 위 모서리만 둥글다.
- 스크롤 시 헤드라인이 먼저 사라지고 카드가 100vw×100vh 확장, 그 후 카피 페이드인(겹침 없음).
- 창 리사이즈 시 시작 위치 어긋남 없음, reflow 없이 부드러움.
- `prefers-reduced-motion`에서 정적 폴백. `media.type` video↔image 교체 동일 동작, 영상 깨지면 poster 폴백.

> 검수 미달 항목은 DeptHero(14B.4) 로직이 아니라 레이아웃 토큰·상수 주입에서 원인을 찾는다(참조 코드는 변경 금지).

---

## Self-Review (작성자 점검 결과)

**1. 스펙 커버리지:** D1 라우트(Task 8·9) / D2 헤더(Task 9) / D3 본문 구성(Task 9, 4·6) / D4 목록 트리(Task 7·8) / D5 미디어·캡션 상수(Task 2) / D6 HeroMedia 공유(Task 3) / D7 fetch·ISR·병렬·notFound(Task 1·9) / D8 카드 비주얼(Task 5) / 타입 네이밍(Task 1) / 테스트(각 Task) / DESIGN.md(Task 10) — 전부 매핑됨.

**2. 플레이스홀더:** 없음(모든 step에 실제 코드·명령·기대값).

**3. 타입·시그니처 일관성:** `DepartmentCardResponse`/`DepartmentDetailResponse`/`DepartmentNode`(Task 1) ↔ 컴포넌트·페이지 사용 일치. `bySortOrder`(Task 1) ↔ Task 9 import 일치. `deptContent`/`deptThumbnail`/`thumbnailOf`/`DEPT_PAGE`(Task 2) ↔ Task 4·5·8·9 사용 일치. DeptHero default export(Task 3) ↔ Task 9 `import DeptHero` 일치. `params: Promise<{id}>` ↔ Next 16 문서 확인.

**토큰 확정:** `globals.css`에서 `--spacing-xxl`(48px)·`--spacing-section`(96px) + 색(primary·surface-soft·ink·muted·hairline) + 타이포(display-md·title-lg·title-md·caption·datetime) 토큰 모두 확인 → `space-y-xxl`·`mt-xxl` 등 사용 유틸 전부 컴파일 보장. 미해결 가정 없음.
